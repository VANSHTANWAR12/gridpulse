# Rules

- **No Git Commands**: Do not run any git commands (such as `git add`, `git commit`, or `git push`). Only modify existing files, write new code, or add new files to the project. The user will handle all git staging, committing, and pushing themselves.
